# Definitions

Pipeline: a staged architectural model for automated test-code refactoring.

Róża: a framework implementation of the staged pipeline.

Preferred sentence:

> We propose a staged pipeline as an architectural model for automated test-code refactoring. Róża implements this model as an extensible framework.
